# graphql-node-simple-boilerplate

1. Replace the `schema.js` file with your `schema.js` file that was generated from [SwitchQL](https://github.com/SwitchQL/SwitchQL)

2. `npm i` 
3. `npm start`
4. Go to `http://localhost:4000/graphql`
